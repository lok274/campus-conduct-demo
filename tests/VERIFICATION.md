# 800+ 學生介面驗證紀錄

日期：2026-09-22。僅測試本機虛構資料，沒有接入真實學生或永久儲存。

## 可重跑的檢查

- `npm test`：11 項 Node 測試；資料組 840 位學生／5,000 筆紀錄。
- `npx tsc --noEmit`、`npm run build`、`git diff --check`。
- `npm run dev` 後開啟 `/?fixture=large`；加上 `&textScale=200` 以根字級 200% 檢查文字重排。
- 以上 URL 選項只在 development 生效；正式建置不含大量資料產生器。一般示範仍為 8 位／8 筆。

## 已執行的本機瀏覽器操作

以下保留 800+ 介面初版的歷史驗證結果；其後按使用者要求移除多項入口及整個學生名冊頁面，現行介面以 AGENTS.md 為準。

- 空白工作台不列學生；按班找到 35 位時快速結果只列 10 位，完整名冊沿用查詢／班級。
- 同名測試生 TEST0001／TEST0037 能以班級、座號、學號區分；小寫完整學號可精確找到一位。
- 25／50／100 分頁；35 人的第 2 頁為 26–35；840 人每頁 100 的末頁為 801–840，下一頁停用。零結果為 0–0，清除條件回到第一頁。
- 名冊第 2 頁開啟學生、個案核對及關閉後，班級及頁碼保留。兩次開關詳情前後，window scrollY 與表格 scrollTop 完全相同。
- 全域搜尋快速結果為 8 項，完整學生入口為 840 位、完整紀錄為全體筆數；Ctrl/Cmd K、方向鍵、Enter、Escape 可操作，開啟學生後返回原紀錄頁。
- 一般新增沒有預選學生；Code 與說明已填妥，仍不能在未選學生時儲存。選人後切換班級會清除學生、重新停用儲存。
- 從 TEST0070 登記帶入正確身分；Code 117 預設 -3，選項順序 -3／-4／-5／-6。實際 -5 儲存後一致；返回更正為 -4 後維持已結案；重新開啟、加入跟進、填摘要結案及確定返回均正常。
- 批次跨兩頁選 50 位，切班不丟失；全選另班 35 位先確認合併後 85 位。取消不改選取，確認才選；全校 840 人核對也僅渲染當頁 25 人。
- 實建 25 筆後，再選 35 人相同 Code、-4、日期及說明：核對指出 25 筆完全重複；取消略過可建 35，恢復略過只新增 10。總紀錄由 5,000 → 5,025 → 5,035，核對第 2 頁可查看新增的 10 人。
- 1440px 桌面、768px 平板、390px 手機進行視覺／操作檢查；手機 200% 文字的名冊、表單及分頁沒有橫向溢出。這是根字級放大測試，不是瀏覽器原生縮放。
- 純搜尋基準：40 次完整 5,000 筆搜尋與分頁約 679ms（當次本機結果，非跨裝置效能保證）。

## 移除跨全部結果全選的回歸驗證（2026-09-22）

- 截圖中的「選取全部符合條件」按鈕及專用確認介面已移除；本機瀏覽器找不到該按鈕。
- 840 位虛構學生：第 1 頁全選 25 位，翻至第 2 頁仍保留 25 位；全選第 2 頁後共 50 位，取消本頁選取後剩 25 位。
- `npm test` 12 項通過；型別檢查、正式建置及差異空白檢查通過。

## 移除總覽學生搜尋咭片的回歸驗證（2026-09-23）

- 總覽找不到「找學生，登記紀錄」咭片，頁首仍顯示全域搜尋；搜尋 `12` 可找到李浩然及相關紀錄。
- 「新增紀錄」仍顯示「搜尋並選擇學生」，沒有預選學生，原有校本 Code、日期、內容及跟進欄位均保留。
- `npm test` 12 項及 TypeScript 型別檢查通過；正式建置及發布前差異檢查另按當次結果確認。

## 移除座號搜尋條件的回歸驗證（2026-09-23）

- 學生、批次、全域及紀錄搜尋的共用文字索引不再包含座號；座號仍保留在結果顯示及班級座號排序。
- 測試加入只出現在 `seat` 欄位的 `SEAT-ZZ`，確認學生及紀錄搜尋均不會命中。
- 大量資料預覽的「測試1B」中，搜尋座號 `01` 顯示 0 位；改搜學號 `TEST0036` 則正確顯示該班座號 01 的學生。

## 移除最近紀錄咭片的回歸驗證（2026-09-23）

- 總覽已移除「最新動態／最近 5 筆紀錄」、五筆紀錄按鈕及「全部紀錄」入口；專用 JSX 與 `.activity`／`.list-card`／`.dashboard-grid` 樣式亦已刪除。
- 這一階段曾把優先待辦咭片改為全寬；其後已按要求刪除該咭片及整個待辦中心，現況見下方最新驗證。
- 大量資料預覽當時找不到上述三項舊文字／按鈕；後續版本已不再顯示任何待辦咭片或入口。

## 移除優先待辦咭片的回歸驗證（2026-09-23）

- 總覽已移除「待辦清單／優先處理 5 項」、學生待辦項目及「完整待辦清單」入口；專用 JSX 與 `.follow*`／`.card-heading`／`.empty-inline` 樣式亦已刪除。
- 這一階段曾保留側欄「待辦中心」及完整待辦頁；其後版本已按要求整個移除。
- 大量資料預覽的總覽只保留頁首操作及頁尾；當時保留的待辦頁驗證已被下方「移除待辦中心」結果取代。

## 移除學生名冊列內登記按鈕的回歸驗證（2026-09-23）

- 學生名冊每列已移除「登記紀錄」按鈕及其 `onAddEntry` 屬性和專用樣式，只保留「查看資料」。
- 大量資料預覽的首 25 位學生均只顯示「查看資料」；個別學生詳情內的新增入口已在後續調整刪除。

## 移除學生詳情新增紀錄按鈕的回歸驗證（2026-09-23）

- 個別學生詳情底部的「為此學生新增紀錄」按鈕及整個底部操作區已刪除，`addEntry` 不再接受預選學生 ID，`.btn.wide` 專用樣式亦已移除。
- 總覽頁首「新增紀錄」保留，開啟表單時沒有預選學生，仍須由老師明確搜尋及選擇學生。

## 移除學生名冊頁面的回歸驗證（2026-09-23）

- 側欄「學生名冊」、`page: "students"`、頁面標題與副標題、名冊 JSX、搜尋／班級／跟進篩選、排序、分頁狀態及 `StudentDirectory` 元件已刪除。
- 全域搜尋仍顯示最多 8 項學生／紀錄快速結果，學生結果可開啟個人詳情；「完整學生結果」入口已刪除，只保留「完整紀錄結果」。
- 新增紀錄的學生搜尋選擇器及批次學生選擇器保留，學生資料模型沒有刪除。

## 緊密學生選擇名單的回歸驗證（2026-09-23）

- 單筆新增的學生搜尋結果不再顯示座號、學號或獨立「選擇學生」按鈕；結果按學生資料的班別分組，以單欄直式排列，每項只顯示姓名並可直接點選。
- 選中學生後仍顯示姓名、班別、座號及學號供老師核對，沒有改動學生與班別的資料對應。

## 合併單人及多人新增流程（2026-09-23）

- 總覽已刪除獨立「批次建立」按鈕，只保留「新增紀錄」；新建一律進入同一個三步驟流程。
- 選 1 位學生會建立 1 筆紀錄，勾選多位會為每位學生建立獨立紀錄；跨頁／跨班選取、重複檢查、Code、分數、核對及短暫復原保留。
- 選生名單按班別分組並以單欄直式顯示姓名；核對頁仍顯示班別、座號及學號。原有單筆新建分支已移除，編輯既有紀錄的表單保留。

## 移除待辦中心功能（2026-09-23）

- 側欄「待辦中心」、`page: "todos"`、頁面標題／副標題、導覽數字、待辦摘要、清單、搜尋、篩選、排序及分頁狀態已全部刪除。
- `TodoCenter`、`TodoMetric`、`compareTodoPriority`、對應排序測試及全部 `.todo-*`／`.nav-count` 樣式已刪除；搜尋原始碼只剩 AGENTS.md 的移除約定及本驗證紀錄。
- 個案詳情中的需要跟進、安排負責人／期限、加入跟進記錄、結案及重新開啟仍保留；這些是紀錄歷史，不是待辦中心頁面。
- `npm test` 11 項全部通過；TypeScript 型別檢查、正式建置及 `git diff --check` 通過。

## 統一個案離開確認（2026-09-25）

- 個案面板的 X、遮罩與 Escape 已共用同一個關閉流程；有尚未儲存的安排、跟進記錄、結案摘要或直接完結原因時，都會先確認。
- 「返回更正」與「直接完結」也使用同一套確認文字及判斷；取消確認會留在原面板並保留剛輸入的內容。

## 真正的最後修改時間（2026-09-25）

- 每筆紀錄加入 ISO 格式的 `updatedAt`；建立、更正、跟進安排、開始跟進、加入跟進、結案、重開及批次更新都會在實際變更時更新。
- 排序選項改為「最後修改」，直接依 `updatedAt` 排序；更正舊紀錄後會排到最前，而不是只看紀錄、跟進或結案日期。
- CSV／JSON 備份格式升級為第 2 版並包含最後修改時間；第 1 版舊備份仍可匯入，系統會從已有紀錄、跟進及結案時間推算初始值。

## 邊界

這些測試不是完整自動化 E2E 套件；未測正式資料庫、多人操作、老師權限或持久化。跟進日期下限限制仍按 AGENTS.md 的已知事項處理。
