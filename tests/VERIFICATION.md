# 800+ 學生介面驗證紀錄

日期：2026-09-22。僅測試本機虛構資料，沒有接入真實學生或永久儲存。

## 可重跑的檢查

- `npm test`：12 項 Node 測試；資料組 840 位學生／5,000 筆紀錄。
- `npx tsc --noEmit`、`npm run build`、`git diff --check`。
- `npm run dev` 後開啟 `/?fixture=large`；加上 `&textScale=200` 以根字級 200% 檢查文字重排。
- 以上 URL 選項只在 development 生效；正式建置不含大量資料產生器。一般示範仍為 8 位／8 筆。

## 已執行的本機瀏覽器操作

以下保留 800+ 介面初版的歷史驗證結果；其後按使用者要求移除跨全部結果的全選按鈕、確認介面及總覽「找學生，登記紀錄」咭片，現行介面以 AGENTS.md 為準。

- 空白工作台不列學生；按班找到 35 位時快速結果只列 10 位，完整名冊沿用查詢／班級。
- 同名測試生 TEST0001／TEST0037 能以班級、座號、學號區分；小寫完整學號可精確找到一位。
- 25／50／100 分頁；35 人的第 2 頁為 26–35；840 人每頁 100 的末頁為 801–840，下一頁停用。零結果為 0–0，清除條件回到第一頁。
- 名冊第 2 頁開啟學生、登記、個案核對及關閉後，班級及頁碼保留。兩次開關詳情前後，window scrollY 與表格 scrollTop 完全相同。
- 全域搜尋快速結果為 8 項，完整學生入口為 840 位、完整紀錄為全體筆數；Ctrl/Cmd K、方向鍵、Enter、Escape 可操作，開啟學生後返回原紀錄頁。
- 一般新增沒有預選學生；Code 與說明已填妥，仍不能在未選學生時儲存。選人後切換班級會清除學生、重新停用儲存。
- 從 TEST0070 登記帶入正確身分；Code 117 預設 -3，選項順序 -3／-4／-5／-6。實際 -5 儲存後一致；返回更正為 -4 後維持已結案；重新開啟、加入跟進、填摘要結案及確定返回均正常。
- 批次跨兩頁選 50 位，切班不丟失；全選另班 35 位先確認合併後 85 位。取消不改選取，確認才選；全校 840 人核對也僅渲染當頁 25 人。
- 實建 25 筆後，再選 35 人相同 Code、-4、日期及說明：核對指出 25 筆完全重複；取消略過可建 35，恢復略過只新增 10。總紀錄由 5,000 → 5,025 → 5,035，核對第 2 頁可查看新增的 10 人。
- 1440px 桌面、768px 平板、390px 手機進行視覺／操作檢查；手機 200% 文字的名冊、表單及分頁沒有橫向溢出。這是根字級放大測試，不是瀏覽器原生縮放。
- 純搜尋基準：40 次完整 5,000 筆搜尋與分頁約 679ms（當次本機結果，非跨裝置效能保證）。

## 移除跨全部結果全選的回歸驗證（2026-09-22）

- 截圖中的「選取全部符合條件」按鈕及專用確認介面已移除；本機瀏覽器找不到該按鈕。
- 840 位虛構學生：第 1 頁全選 25 位，翻至第 2 頁仍保留 25 位；全選第 2 頁後共 50 位，取消本頁選取後剩 25 位。
- `npm test` 12 項通過；型別檢查、正式建置及差異空白檢查通過。

## 移除總覽學生搜尋咭片的回歸驗證（2026-09-23）

- 總覽找不到「找學生，登記紀錄」咭片，頁首仍顯示全域搜尋；搜尋 `12` 可找到李浩然及相關紀錄。
- 「新增紀錄」仍顯示「搜尋並選擇學生」，沒有預選學生，原有校本 Code、日期、內容及跟進欄位均保留。
- `npm test` 12 項及 TypeScript 型別檢查通過；正式建置及發布前差異檢查另按當次結果確認。

## 移除座號搜尋條件的回歸驗證（2026-09-23）

- 學生、批次、全域及紀錄搜尋的共用文字索引不再包含座號；座號仍保留在結果顯示及班級座號排序。
- 測試加入只出現在 `seat` 欄位的 `SEAT-ZZ`，確認學生及紀錄搜尋均不會命中。
- 大量資料預覽的「測試1B」中，搜尋座號 `01` 顯示 0 位；改搜學號 `TEST0036` 則正確顯示該班座號 01 的學生。

## 邊界

這些測試不是完整自動化 E2E 套件；未測正式資料庫、多人操作、老師權限或持久化。個案已有的離開確認及跟進日期下限限制仍按 AGENTS.md 的已知事項處理。
